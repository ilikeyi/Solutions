ConvertFrom-StringData -StringData @'
	# th-TH
	# Thai (Thailand)

	OSVersion                 = เลือกหมายเลขเวอร์ชันที่ทราบ
	DownloadAll               = ดาวน์โหลดทั้งหมด
	DownloadChannel           = ช่องทาง
	ChannelWIF                = ตัวอย่าง Windows Insider Fast Ring
	ChannelWIS                = ตัวอย่าง Windows Insider Slow Ring
	ChannelRP                 = ตัวอย่างเวอร์ชันอย่างเป็นทางการ
	ChannelRetail             = เวอร์ชันขายปลีก
	StartVerify               = ตรวจสอบงานดาวน์โหลด
	LXPsFilter                = กรอง
	LXPsDownloadTips          = เลือกหมายเลขเวอร์ชันที่รู้จัก หรือป้อนหมายเลขเวอร์ชันที่เกี่ยวข้องด้วยตนเองเพื่อกรอง ตัวเลขต้องมีความยาวไม่น้อยกว่า 5 หลัก และต้องไม่มี: ตัวอักษรภาษาอังกฤษ ช่องว่างนำหน้าและต่อท้าย, \\ / : * ? & @ ! "" < > | .
	ISO9660TipsErrorSpace     = ไม่สามารถมี: ช่องว่างนำหน้าและต่อท้าย
	ISO9660TipsErrorAZ        = ไม่สามารถมี: ตัวอักษร A-Z
	ISO9660TipsErrorOther     = ไม่สามารถมี: \\ / : * ? & @ ! "" < > |
	ISOLengthError            = ความยาวต้องไม่เกิน {0} อักขระ
	ISOShortError             = ความยาวต้องไม่น้อยกว่า {0} อักขระ
	NoSetLabel                = โปรดป้อนหมายเลขเวอร์ชันด้วยตนเอง
	LXPsRename                = เปลี่ยนชื่อโดยอัตโนมัติตามกฎ
	LXPsRenameTips            = ตัวอย่าง: LanguageExperiencePack.{*}.Neutral.appx
	LicenseCreate             = สร้างใบรับรอง License.xml
	LicenseCreateTips         = ค้นหาตามแท็กภูมิภาค ไม่มีไฟล์ใบรับรอง License.xml ปล่อย License.xml จาก LanguageExperiencePack.{*}.Neutral.appx

	SaveTo                    = บันทึกไปที่
	SelectFolder              = เลือกไดเรกทอรี
	SelectFolderTips          = หลังจากตั้งค่าไดเรกทอรีใหม่ ตัวเลือก "ซิงค์ตำแหน่งต้นทางที่สอดคล้องกับการดาวน์โหลดไปยังตำแหน่ง" จะถูกปิด
	SaveToSync                = ตำแหน่งต้นทางการซิงโครไนซ์สอดคล้องกับตำแหน่งการดาวน์โหลด
	SaveToSyncTips            = หลังจากตั้งค่าหมายเลขเวอร์ชันการกรองใหม่ ให้ซิงโครไนซ์การเปลี่ยนแปลงใหม่กับตำแหน่งไดเร็กทอรีบันทึกเดียวกัน โปรดปิดฟังก์ชันนี้หลังจากปรับแต่งแล้ว
	OpenFolder                = เปิดไดเรกทอรี
	Paste                     = คัดลอกเส้นทาง
	AdvAppsDetailed           = สร้างรายงาน
	AdvAppsDetailedTips       = ค้นหาตามแท็กภูมิภาค รับรายละเอียดเพิ่มเติมเมื่อมีชุดประสบการณ์ภาษาท้องถิ่น และสร้างไฟล์รายงาน: *.CSV
	AddSources                = เพิ่มแหล่งที่มา:
	AddQueue                  = เพิ่มคิว:
	YesWork                   = มีงานเข้าคิว
	NoWork                    = ไม่มีงาน
	ProcessSources            = แหล่งประมวลผล
	AvailableLanguages        = ภาษาที่ใช้ได้
	Not_Select                = ไม่มีภาษาที่เลือกไว้
	MatchDownloadNoNewitem    = ไม่ตรงกัน
	MatchNoDownloadItem       = จับคู่รายการที่ไม่ได้ดาวน์โหลด
	Get_Link_Failed           = ไม่สามารถรับลิงก์ได้ โปรดตรวจสอบ URL ของร้านค้า
	AlreadyExists             = ดาวน์โหลดแล้ว
	DownloadNow               = กำลังดาวน์โหลด
	DownloadFailed            = การดาวน์โหลดล้มเหลว
	Path_Invalid_Failed       =	อักขระไม่ถูกต้องในเส้นทาง

	LXPsAddDelTipsView        = มีเคล็ดลับใหม่ๆ โปรดตรวจสอบเลย
	LXPsAddDelTips            = อย่าแจ้งเตือนอีก
	LXPsGetSNTips             = จะรับหมายเลขเวอร์ชันที่ตรงกันได้อย่างไร\n\n   1. เปิด URL: https://store.rg-adguard.net\n\n   2. คัดลอกลิงค์ต่อไปนี้และวางลงใน https://store.rg-adguard.net ในกล่องค้นหา คลิกค้นหาเพื่อค้นหาหมายเลขเวอร์ชันที่ทราบที่ตรงกัน\n\n       English (United States)\n       https://www.microsoft.com/store/productId/9PDSCC711RVF\n\n       Chinese (Simplified, China)\n       https://www.microsoft.com/store/productId/9NRMNT6GMZ70\n\n       Chinese (Traditional, Taiwan)\n       https://www.microsoft.com/store/productId/9PCJ4DHCQ1JQ\n\n       Japanese (Japan)\n       https://www.microsoft.com/store/productId/9N1W692FV4S1\n\n       Korean (Korea)\n       https://www.microsoft.com/store/productId/9N4TXPCVRNGF\n\n\n   กรุณาคลิก "ดูรายละเอียด" เพื่อเรียนรู้เพิ่มเติม
'@