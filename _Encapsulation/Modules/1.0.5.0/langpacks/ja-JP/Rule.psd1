ConvertFrom-StringData -StringData @'
	# ja-JP
	# Japanese (Japan)

	Language                        = 言語
	LanguageReset                   = 言語設定をリセットする
	LanguageCode                    = ゾーンマーカー
	LanguageShort                   = 短い領域マーカー
	LanguageRegionLink              = 地域協会
	LanguageNoSelect                = 画像ソースを選択した後, 既知の言語を表示する
	LanguageMatchError              = 言語の一致に失敗しました。デフォルトです。言語ボタンをクリックして変更してください。
	LanguageChangeError             = 変更を加える前に, 画像ソースを選択してください。
	LanguageExtract                 = 抽出
	LanguageExtractRule             = 抽出ルール
	LanguageExtractRuleFilter       = フィルタルール
	LanguageExtractRuleNoSel        = 抽出ルールの種類を選択してください，Install また boot プログラム。
	LanguageExtractCondition        = 一致は, 必要な前提条件パッケージに適用されます
	LanguageExtractConditionTips    = 言語を選択し, [取得]ボタンをクリックして, 検索条件の結果を表示します。\n\n検索条件：完全検索, トラバーサルなし, ディレクトリ構造に基づく判断のみ。
	LanguageExtractSearchResult     = の検索結果
	LanguageExtractSearchResultNO   = 一致する結果はありません
	LanguageExtractSearchResultTips = 言語を選択し, [検索して一致]ボタンをクリックして, 追加する言語のコンテンツが一貫しているかどうかを確認します。同じである場合は, [インポート]ボタンをクリックしてインスタント結果を表示します。
	LanguageExtractSearch           = 検索して照合
	LanguageExtractAddTo            = 輸入
	LanguageExtractTo               = インポート後, 次の場所に追加します：
'@