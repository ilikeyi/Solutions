ConvertFrom-StringData -StringData @'
	# zh-CN
	# Chinese (Simplified, China)

	Language                        = 语言
	LanguageReset                   = 重置语言设置
	LanguageCode                    = 区域标记
	LanguageShort                   = 短区域标记
	LanguageRegionLink              = 区域关联
	LanguageNoSelect                = 选择映像源后显示已知语言
	LanguageMatchError              = 匹配语言失败，已初始为默认，请点击语言按钮更改。
	LanguageChangeError             = 更改前，请选择映像源。
	LanguageExtract                 = 提取
	LanguageExtractRule             = 提取规则
	LanguageExtractRuleFilter       = 筛选规则
	LanguageExtractRuleNoSel        = 请选择提取规则类型，Install 或 boot 方案。
	LanguageExtractCondition        = 匹配适用于所需的必备包
	LanguageExtractConditionTips    = 选择语言，点击“获取”按钮即可显示搜索条件结果。\n\n搜索条件：全盘搜索，不会遍历，只按目录结构判断。
	LanguageExtractSearchResult     = 搜索结果
	LanguageExtractSearchResultNO   = 未匹配到的结果
	LanguageExtractSearchResultTips = 选择语言，点击“搜索并匹配”按钮后，查看是否你要添加的语言内容一致，如果一致，点击“导入”按钮即可显示即时结果。
	LanguageExtractSearch           = 搜索并匹配
	LanguageExtractAddTo            = 导入
	LanguageExtractTo               = 导入后添加到：
'@